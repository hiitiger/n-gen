const WEBAPP_EVENT = {
    APP: {
        QUIT: "APP.QUIT",
    },
};

const CLIENT_EVENT = {
    WINDOW: {
        CLOSED: "CLIENT.WINDOW.CLOSED",
    },
};

export { WEBAPP_EVENT, CLIENT_EVENT };
